# UI (shared)

- `app_theme.tres` — the single shared Theme (Phase 0). All screens use it; no per-screen styling.
- `hud_strip.tscn` (Phase 2) — persistent Energy/Focus/Mood + Level strip visible from every screen.
- `main_menu.tscn`, login/signup screens (Phase 1), Codex view (Phase 3), scheduler badge (Phase 4).

Current working room `scenes/ui/main.tscn` moves here during Phase 2 migration.
